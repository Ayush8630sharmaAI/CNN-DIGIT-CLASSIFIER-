# CNN-DIGIT-CLASSIFIER-
A CNN-based handwritten digit classifier built with PyTorch. Trained on the MNIST dataset using convolutional layers, BatchNorm, MaxPooling, Dropout, and Adam optimizer. Achieved 99.23% test accuracy. This project demonstrates the complete deep learning workflow from training to inference.
